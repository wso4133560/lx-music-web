## 1. 平台能力梳理与抽象

- [x] 1.1 盘点 `/home/tanglin/workspace2/lx-music-web/src/renderer` 中对 `src/renderer/utils/ipc.ts`、`window.lx`、Electron preload 与桌面运行时全局对象的直接依赖
- [x] 1.2 按能力域拆分平台接口，至少覆盖设置、同步、下载、外部动作与运行时初始化
- [x] 1.3 为平台接口定义 Web 实现与桌面实现的装配入口，确保渲染层不再直接依赖 Electron IPC 包装函数

## 2. 渲染层启动链路 Web 化

- [x] 2.1 调整 `/home/tanglin/workspace2/lx-music-web/src/renderer/main.ts` 的启动流程，使其在没有 Electron preload 和主进程 IPC 的情况下仍可挂载应用
- [x] 2.2 替换渲染层核心启动链路中对桌面专属运行时的直接访问，保证 Web 运行时不会在启动阶段导入 Electron 相关对象
- [x] 2.3 验证第一阶段 Web 版本保留的路由主路径仍覆盖搜索、榜单、歌单列表与歌单详情

## 3. 第一阶段 Web 核心能力迁移

- [x] 3.1 将搜索、在线播放、榜单、歌单列表与歌单详情所需能力切换到新的平台接口
- [x] 3.2 将基础设置读取、更新与持久化切换到 Web 兼容的设置运行时
- [x] 3.3 明确移除第一阶段 Web 版本中的本地音乐入口与依赖路径

## 4. 同步与下载 API 化

- [x] 4.1 为歌单同步定义前后端 API 契约，并将前端调用从 Electron IPC 迁移到 Web 可消费接口
- [x] 4.2 为下载能力定义前后端 API 或下载端点契约，并将前端调用从 Electron IPC 迁移到 Web 可消费接口
- [x] 4.3 为同步与下载补齐首阶段成功路径验证，确认它们仍属于第一阶段 Web 支持范围

## 5. 桌面专属能力隔离

- [x] 5.1 清理或隔离托盘、全局快捷键、桌面歌词、自动更新、deeplink/协议注册、Electron 窗口特性在 Web 主路径中的入口
- [x] 5.2 确保桌面专属设置项不再成为第一阶段 Web 运行必需项
- [x] 5.3 验证 Web 主路径在不启用任何桌面专属能力时可独立运行

## 6. 收尾与验证

- [x] 6.1 验证第一阶段 Web 主闭环可完成搜索、在线播放、榜单浏览、歌单浏览、基础设置、同步与下载
- [x] 6.2 复查 `src/renderer` 中是否仍存在第一阶段不允许残留的 Electron IPC 直接调用路径
- [x] 6.3 补充必要的迁移说明，明确第一阶段排除项与后续阶段扩展点
